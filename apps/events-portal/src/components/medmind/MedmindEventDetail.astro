---
import { MedmindEventDetail as MedmindEventDetailReact } from './MedmindEventDetail';

interface Props {
  institution: string;
  event: any;
  edition: any;
  speakers: any[];
  activities: any[];
  form: any;
  heroImage: string;
  heroVideo?: string;
  apiUrl: string;
}

const props = Astro.props;
---

<MedmindEventDetailReact client:load {...props} />
